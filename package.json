{
  "name": "vanilla-markdown-blog",
  "version": "1.0.0",
  "description": "Static site generator for a vanilla HTML, CSS, and JavaScript markdown blog.",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "preview": "npm run build && npx serve docs -l 3000"
  },
  "keywords": [
    "static-site-generator",
    "markdown",
    "blog",
    "vanilla"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^9.1.5"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
